shrec17_14 on CTRGCN with seed= 1, we got top1= 96.19%, top5= 100.0%, Mean test loss= 0.15445
shrec17_14 on CTRGCN with seed= 2, we got top1= 96.79%, top5= 100.0%, Mean test loss= 0.12253
SHREC 3-seed result (96.51% ± 0.30)

# Week 2 — Day 2 Report (Friday May 29)

## Objectives status
- Correctness gate: IN PROGRESS. before_backbone + CFOT run relaunched on
  A5000 (seed 1, NTU-60) after a power-cut interrupted the first attempt.
  Restarted clean from epoch 0 (an interrupted run cannot certify the
  abstraction). Result lands Day 3. Pass band defined: must reproduce the
  Week-1 regression — accuracy BELOW the 89.55% baseline, sign NEGATIVE,
  roughly -0.45 pp. Positive/wild => abstraction unfaithful, halt pilot.
- SHREC: CLOSED. 3-seed baseline = 96.51% ± 0.30 (seeds 0/1/2), exceeds
  published 96.10%. Objective 2 fully done at quality bar.
- IPN-13 repair: npz BUILT + VERIFIED (pulled forward from Day 3).

## Done today
- Mixer scaffolding (Day-1 deliverable) confirmed sound; correctness-gate
  run launched against it.
- SHREC seeds 1,2 completed -> 3-seed mean.
- IPN protocol decision RESOLVED with evidence: confirmed via ST-SGCN
  (Ikne et al., FG 2024) — isolated classification, 13 classes, no-gesture
  excluded, reported 89.0% on IPN. The Week-1 "drop no-gesture" instruction
  is therefore correct and now citation-grounded, not assumed.
- Normalization decision RESOLVED with evidence: read ST-SGCN's IPN data
  code — they use RAW MediaPipe coords, no spatial centering/normalization.
  So IPN13.npz generated raw (parallels the SHREC uncentering win; avoids
  repeating the palm-center -3.3pp mistake).
- IPN13.npz generated by wrapping ST-SGCN's own IPNDataset (byte-identical
  parsing): train 2493 / val 624 / test 1030, layout (N,4,80,21,1), labels
  0-12. Slice [:3] at load for xyz.
- Bug found + fixed in interpolate_landmarks: output buffer channel dim was
  hardcoded to 6 (leftover from a motion-feature variant); changed to use
  input's actual channel count. Pre-existing ST-SGCN code bug.

## Known issues / notes for the record
- IPN class imbalance is INTRINSIC (verified against dataset source): classes
  0,1 (the dominant pointing gestures) ~5x larger than the 11 action classes
  (~595 vs ~118 train each). Implication: report macro-F1 / per-class
  alongside top-1; expect residual seed variance from the 11 small classes.
- Paper-vs-code gap, documented: ST-SGCN paper says T=80 for IPN, their code
  sets max_seq_len=60. Used T=80 (matches the cited 89.0%); T=60 is fallback.
- mediapipe not installed in `msg3d` env -> connectivity channel used fallback
  values. Irrelevant to xyz-only training (channel sliced off), but install
  mediapipe if the connectivity feature is ever activated.

## Blocker before IPN training
- Need a 21-joint MediaPipe hand graph (graph/ipn_hand.py). Existing graphs
  are NTU-25 and SHREC-22. Build + verify non-empty before launching ST-GCN.

## Next (Day 3)
- Read correctness-gate result against the negative-sign band. PASS -> pilot
  cleared. FAIL -> debug mixer config (must match the 95.32% CFOT setup).
- Build/verify graph/ipn_hand.py; launch IPN-13 ST-GCN seed 0 on 5050.
- (If gate passes) begin placement pilot prep on A5000.